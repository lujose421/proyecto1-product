server:
  port: 8080

spring:
  cloud:
    config:
      enable: false
      impor-check:
        enable: false
  application:
    name: customer
  data:
    mongodb:
      uri: mongodb+srv://pacherresfloresjose12:<password>@cluster0.5gmvqd9.mongodb.net/dbcustomer

springdoc:
  swagger-ui:
    #    enabled: true
    path: index.html

#logging:
#  level:
#    root: INFO
#    com.nttdata.customer: DEBUG
#  file:
#    name: logs/app.log
#  pattern:
#    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
#    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
